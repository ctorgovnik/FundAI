Look at GitHub issue #$ARGUMENTS in ctorgovnik/FundAI using the GitHub MCP. Read the full issue body — all tasks, acceptance criteria, dependencies, and notes.

Then implement it:
- Follow all conventions in the root CLAUDE.md and the relevant subdirectory CLAUDE.md
- Write the code for every task checkbox
- Write tests covering the acceptance criteria
- Make sure the build passes (`cmake --build build && ctest --test-dir build`)

When done, summarize: what you built, what tests pass, and anything that was deferred or needs follow-up.
